# Environments & Release Lifecycles

Core question: What happens between "code merged" and "users see the change"?

Covers:
dev/staging/prod 
Environment parity 
Artifacts
Config vs code
Basic release flow (build → test → deploy → verify → monitor)

CFU Ideas:
What’s the main purpose of having separate environments like dev, staging, and production?
In a typical release flow, which step should happen before deploying to production?
Which statement best describes an “artifact” in a deployment pipeline?